import React from 'react';
import { connect } from 'react-redux';

const Events = () => (
    <div className="Events">
        
    </div>
);

export default connect (
    state => ({

    }),
    dispatch => ({

    }),
)(Events);